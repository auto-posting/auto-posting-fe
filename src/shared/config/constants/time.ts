export const DATE = Array.from({ length: 31 }, (_, i) => (i + 1).toString());

export const HOUR = Array.from({ length: 12 }, (_, i) => (i + 1).toString());

export const MINUTE = Array.from({ length: 60 }, (_, i) => (i + 1).toString());
